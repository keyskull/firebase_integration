# This is a basic workflow to help you get started with Actions


# Controls when the action will run.
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ develop ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Flutter Environment
        uses: subosito/flutter-action@v1
        with:
          channel: 'stable'
      - name: Build Web Page
        uses: keyskull/flutter-gh-pages@v4
        with:
          targetBranch: gh-pages-develop
  deploy:
    if: always()
    needs: Build
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - - name: Write File
          uses: DamianReeves/write-file-action@v1.0
          with:
            path: ${{github.workspace}}/CNAME
            contents: |
              dragon-ball.ml
            write-mode: overwrite
      - name: Files Sync Action
        uses: adrianjost/files-sync-action@v1.0.1
        with:
          FILE_PATTERNS: |
            \S
          TARGET_REPOS: |
            keyskull/cullen.ml_test
          GITHUB_TOKEN: ${{secrets.SYNCED_GITHUB_TOKEN}}